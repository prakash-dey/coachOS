import Link from "next/link";
import { createWorkoutPlan } from "../actions";

export default async function NewWorkoutPlanPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  const query = await searchParams;
  return <main className="px-4 py-6 sm:px-6 lg:px-10 lg:py-9"><div className="mx-auto max-w-3xl"><Link href="/workout-plans" className="text-sm font-medium text-brand">← Plan library</Link><div className="mt-6 overflow-hidden rounded-[2rem] border border-border bg-surface"><div className="bg-brand-strong p-7 text-white sm:p-10"><p className="text-xs font-bold uppercase tracking-[.2em] text-accent">New training system</p><h1 className="mt-3 text-3xl font-semibold">Start with the intent.</h1><p className="mt-2 max-w-xl text-sm leading-6 text-white/65">Name the outcome this plan creates. You’ll add training days and exercises next.</p></div><form action={createWorkoutPlan} className="space-y-6 p-7 sm:p-10">{query.error && <p className="rounded-xl bg-red-50 p-4 text-sm text-red-700">We couldn’t create this plan. Check the details and try again.</p>}<div><label htmlFor="name" className="text-sm font-semibold">Plan name</label><input id="name" name="name" required maxLength={120} placeholder="e.g. 12-week strength foundation" className="mt-2 min-h-12 w-full rounded-xl border border-border bg-background px-4 outline-none focus:border-brand focus:ring-2 focus:ring-brand/10" /></div><div><label htmlFor="description" className="text-sm font-semibold">Coaching intent</label><textarea id="description" name="description" maxLength={5000} rows={5} placeholder="Who is it for, and what will it help them achieve?" className="mt-2 w-full rounded-xl border border-border bg-background p-4 outline-none focus:border-brand focus:ring-2 focus:ring-brand/10" /></div><div className="flex flex-wrap gap-3"><button className="min-h-11 rounded-full bg-brand px-6 text-sm font-semibold text-white">Create and add days</button><Link href="/workout-plans" className="inline-flex min-h-11 items-center rounded-full px-5 text-sm font-semibold text-muted">Cancel</Link></div></form></div></div></main>;
}
