import { redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server";
export default async function ClientNutritionPage() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/login"); const { data: client } = await supabase.from("clients").select("id").eq("user_id", user.id).maybeSingle(); if (!client) redirect("/auth/continue"); const { data: assignment } = await supabase.from("nutrition_plan_assignments").select("starts_on, nutrition_plans(name, description, daily_calories, protein_grams, carbs_grams, fat_grams, nutrition_meals(id, name, timing, position, nutrition_items(id, name, amount, alternatives, position)))").eq("client_id", client.id).eq("status", "active").order("starts_on", { ascending:false }).limit(1).maybeSingle(); const plan = assignment?.nutrition_plans as unknown as {name:string;description:string|null;daily_calories:number|null;protein_grams:number|null;carbs_grams:number|null;fat_grams:number|null;nutrition_meals:Array<{id:string;name:string;timing:string|null;position:number;nutrition_items:Array<{id:string;name:string;amount:string;alternatives:string|null;position:number}>}>}|null; return <main className="px-4 py-7 sm:px-6 lg:py-10"><div className="mx-auto max-w-5xl"><header><p className="text-xs font-bold uppercase tracking-[.2em] text-[#9a4a21]">Your nutrition</p><h1 className="mt-2 text-3xl font-semibold sm:text-4xl">{plan?.name ?? "No nutrition plan assigned"}</h1><p className="mt-2 text-muted">{plan?.description || "Your coach will share food guidance here."}</p></header>{plan && <><section className="mt-8 grid grid-cols-2 gap-3 sm:grid-cols-4">{[["Energy",plan.daily_calories,"kcal"],["Protein",plan.protein_grams,"g"],["Carbs",plan.carbs_grams,"g"],["Fat",plan.fat_grams,"g"]].map(([label,value,unit],i)=><div key={String(label)} className={`rounded-[1.5rem] p-5 ${["bg-brand-strong text-white","bg-[#e7ebff]","bg-[#fff0e7]","bg-[#e4f4de]"][i]}`}><p className="text-xs font-bold uppercase tracking-wider opacity-60">{label}</p><p className="mt-2 text-2xl font-semibold">{value ?? "—"} <span className="text-xs">{unit}</span></p></div>)}</section><div className="mt-5 space-y-5">{plan.nutrition_meals.sort((a,b)=>a.position-b.position).map((meal,index)=><article key={meal.id} className="overflow-hidden rounded-[2rem] border border-border bg-surface"><header className="flex items-center gap-4 bg-[#9a4a21] px-6 py-5 text-white"><span className="grid h-10 w-10 place-items-center rounded-full bg-[#ffd8bd] font-bold text-[#7a3517]">{index+1}</span><div><h2 className="font-semibold">{meal.name}</h2><p className="text-xs text-white/60">{meal.timing || "Flexible timing"}</p></div></header><div className="divide-y divide-border">{meal.nutrition_items.sort((a,b)=>a.position-b.position).map(item=><div key={item.id} className="flex items-start justify-between gap-4 px-6 py-4"><div><p className="font-semibold">{item.name}</p>{item.alternatives && <p className="mt-1 text-xs text-muted">Alternative: {item.alternatives}</p>}</div><span className="shrink-0 rounded-full bg-[#fff0e7] px-3 py-1 text-sm font-bold text-[#9a4a21]">{item.amount}</span></div>)}</div></article>)}</div></>}</div></main>; }
