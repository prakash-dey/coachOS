import Link from "next/link"; import { createNutritionPlan } from "../actions";
import { Button } from "@/app/components/ui/Button";
import { Alert } from "@/app/components/ui/Feedback";
import { Field, Input, Textarea } from "@/app/components/ui/FormControls";
export default async function NewNutritionPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const query = await searchParams; return <main className="px-4 py-6 sm:px-6 lg:px-10 lg:py-9"><div className="mx-auto max-w-3xl"><Link href="/nutrition-plans" className="text-sm font-medium text-brand">← Nutrition library</Link><div className="mt-6 overflow-hidden rounded-[2rem] border border-border bg-surface"><div className="bg-[#9a4a21] p-7 text-white sm:p-10"><p className="text-xs font-bold uppercase tracking-[.2em] text-[#ffd8bd]">New food framework</p><h1 className="mt-3 text-3xl font-semibold">Nutrition without the spreadsheet.</h1><p className="mt-2 text-sm text-white/70">Set the daily direction now, then shape meals and flexible food choices.</p></div><form action={createNutritionPlan} className="space-y-6 p-7 sm:p-10">{query.error && <Alert tone="error">Check the values and try again.</Alert>}<Field label="Plan name" htmlFor="name"><Input id="name" name="name" required maxLength={120} placeholder="Balanced performance plan" /></Field><Field label="Strategy" htmlFor="description"><Textarea id="description" name="description" rows={4} placeholder="How should this plan fit the client’s life?" /></Field><div className="grid grid-cols-2 gap-3 sm:grid-cols-4">{[["calories","Calories"],["protein","Protein (g)"],["carbs","Carbs (g)"],["fat","Fat (g)"]].map(([name,label]) => <Field key={name} label={label} htmlFor={name}><Input id={name} name={name} type="number" min="0" /></Field>)}</div><Button type="submit">Create and add meals</Button></form></div></div></main>; }
