/* eslint-disable @next/next/no-img-element */
import Link from "next/link"; import { notFound, redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server"; import { saveFeedback } from "./actions";
const PHOTO_BUCKET = "check-in-photos";
export default async function ReviewCheckInPage({ params }: { params: Promise<{ id: string; checkInId: string }> }) { const { id, checkInId } = await params; const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/login"); const { data: workspace } = await supabase.from("workspaces").select("id").eq("owner_id", user.id).maybeSingle(); if (!workspace) redirect("/onboarding"); const { data: checkIn } = await supabase.from("check_ins").select("id, week_start, weight_kg, energy_score, mood_score, notes, coach_feedback, progress_photo_path, clients(first_name, last_name)").eq("id", checkInId).eq("client_id", id).eq("workspace_id", workspace.id).maybeSingle(); if (!checkIn) notFound(); let photoUrl: string | null = null; if (checkIn.progress_photo_path) { const { data } = await supabase.storage.from(PHOTO_BUCKET).createSignedUrl(checkIn.progress_photo_path, 600); photoUrl = data?.signedUrl ?? null; } const client = checkIn.clients as unknown as { first_name:string; last_name:string } | null; const action = saveFeedback.bind(null, id, checkInId); return <main className="px-4 py-6 sm:px-6 lg:px-10 lg:py-9"><div className="mx-auto max-w-5xl"><Link href={`/clients/${id}/check-ins`} className="text-sm font-medium text-brand">← Check-in history</Link><header className="mt-5"><p className="text-xs font-bold uppercase tracking-[.2em] text-brand">Weekly review</p><h1 className="mt-2 text-3xl font-semibold">{client ? `${client.first_name} ${client.last_name}` : "Client"}</h1><p className="mt-2 text-muted">Week of {checkIn.week_start}</p></header><div className="mt-8 grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><section className="space-y-4">{photoUrl ? <a href={photoUrl} target="_blank" rel="noreferrer" className="block overflow-hidden rounded-[2rem] bg-surface"><img src={photoUrl} alt={`Progress for week of ${checkIn.week_start}`} className="aspect-[4/5] w-full object-cover"/></a> : <div className="grid aspect-[4/3] place-items-center rounded-[2rem] border border-dashed border-border bg-surface text-sm text-muted">No progress photo</div>}<div className="grid grid-cols-3 gap-3"><div className="rounded-[1.4rem] bg-[#fff0e7] p-4"><p className="text-xs text-muted">Energy</p><p className="mt-1 text-2xl font-semibold">{checkIn.energy_score}/5</p></div><div className="rounded-[1.4rem] bg-[#e7ebff] p-4"><p className="text-xs text-muted">Mood</p><p className="mt-1 text-2xl font-semibold">{checkIn.mood_score}/5</p></div><div className="rounded-[1.4rem] bg-[#e4f4de] p-4"><p className="text-xs text-muted">Weight</p><p className="mt-1 text-xl font-semibold">{checkIn.weight_kg ?? "—"}<small> kg</small></p></div></div></section><section className="rounded-[2rem] border border-border bg-surface p-6 sm:p-8"><div><p className="text-xs font-bold uppercase tracking-wider text-muted">Client reflection</p><p className="mt-3 whitespace-pre-wrap leading-7">{checkIn.notes || "No reflection was added."}</p></div><form action={action} className="mt-8 border-t border-border pt-7"><label htmlFor="feedback" className="text-sm font-semibold">Your coaching response</label><p className="mt-1 text-xs text-muted">Make it specific, useful, and encouraging.</p><textarea id="feedback" name="feedback" required maxLength={3000} rows={8} defaultValue={checkIn.coach_feedback ?? ""} className="mt-3 w-full rounded-2xl border border-border bg-background p-4 outline-none focus:border-brand focus:ring-2 focus:ring-brand/10" placeholder="Celebrate the win, identify the pattern, and suggest the next action…"/><button className="mt-4 min-h-11 rounded-full bg-brand px-6 text-sm font-semibold text-white">Save feedback</button></form></section></div></div></main>; }
