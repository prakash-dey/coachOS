import { redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server";
export default async function ClientWorkoutPage() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/login"); const { data: client } = await supabase.from("clients").select("id").eq("user_id", user.id).maybeSingle(); if (!client) redirect("/auth/continue"); const { data: assignment } = await supabase.from("workout_plan_assignments").select("starts_on, workout_plans(name, description, workout_days(id, name, notes, position, workout_exercises(id, name, sets, reps, rest_seconds, position)))").eq("client_id", client.id).eq("status", "active").order("starts_on", { ascending: false }).limit(1).maybeSingle(); const plan = assignment?.workout_plans as unknown as { name:string; description:string|null; workout_days:Array<{id:string;name:string;notes:string|null;position:number;workout_exercises:Array<{id:string;name:string;sets:number;reps:string;rest_seconds:number|null;position:number}>}> } | null; return <main className="px-4 py-7 sm:px-6 lg:py-10"><div className="mx-auto max-w-5xl"><header><p className="text-xs font-bold uppercase tracking-[.2em] text-brand">Your training</p><h1 className="mt-2 text-3xl font-semibold sm:text-4xl">{plan?.name ?? "No workout assigned"}</h1><p className="mt-2 text-muted">{plan?.description || "Your coach will share your training plan here."}</p></header>{plan ? <div className="mt-8 space-y-5">{plan.workout_days.sort((a,b)=>a.position-b.position).map((day,index) => <article key={day.id} className="overflow-hidden rounded-[2rem] border border-border bg-surface"><header className="flex items-center gap-4 bg-brand-strong px-6 py-5 text-white"><span className="grid h-11 w-11 place-items-center rounded-full bg-accent font-bold text-brand-strong">{index+1}</span><div><h2 className="text-lg font-semibold">{day.name}</h2><p className="text-xs text-white/60">{day.notes || `${day.workout_exercises.length} exercises`}</p></div></header><div className="divide-y divide-border">{day.workout_exercises.sort((a,b)=>a.position-b.position).map((ex,i)=><div key={ex.id} className="grid grid-cols-[2rem_1fr_auto] items-center gap-3 px-5 py-4"><span className="text-xs font-bold text-muted">{String(i+1).padStart(2,"0")}</span><div><p className="font-semibold">{ex.name}</p><p className="text-xs text-muted">{ex.rest_seconds ? `${ex.rest_seconds}s rest` : "Rest as needed"}</p></div><span className="rounded-full bg-background px-3 py-1 text-sm font-bold">{ex.sets} × {ex.reps}</span></div>)}</div></article>)}</div> : <div className="mt-8 rounded-[2rem] border border-dashed border-brand/30 bg-surface p-10 text-center"><p className="text-xl font-semibold">Your training space is ready</p><p className="mt-2 text-sm text-muted">An assigned workout will appear here.</p></div>}</div></main>; }
