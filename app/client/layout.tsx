import { redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server"; import { signOut } from "@/app/auth/actions"; import Link from "next/link";
export default async function ClientLayout({ children }: { children: React.ReactNode }) { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/login"); const { data: membership } = await supabase.from("workspace_members").select("workspace_id, role, status, workspaces(name)").eq("user_id", user.id).maybeSingle(); if (!membership || membership.role !== "client" || membership.status !== "active") redirect("/auth/continue"); const workspace = membership.workspaces as unknown as { name: string } | null; return <div className="min-h-screen bg-background"><header className="border-b border-border bg-surface"><div className="mx-auto flex min-h-16 max-w-7xl items-center justify-between gap-4 px-4 sm:px-6"><Link href="/client" className="flex items-center gap-3 font-semibold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-brand text-sm font-black text-white">C</span><span>CoachOS <small className="ml-1 font-normal text-muted">· {workspace?.name}</small></span></Link><nav className="hidden items-center gap-1 md:flex"><Link href="/client" className="rounded-full px-4 py-2 text-sm font-medium hover:bg-background">Today</Link><Link href="/client/workout" className="rounded-full px-4 py-2 text-sm font-medium hover:bg-background">Workout</Link><Link href="/client/nutrition" className="rounded-full px-4 py-2 text-sm font-medium hover:bg-background">Nutrition</Link><Link href="/client/check-ins" className="rounded-full px-4 py-2 text-sm font-medium hover:bg-background">Check-ins</Link></nav><form action={signOut}><button className="min-h-10 rounded-full border border-border px-4 text-sm font-semibold">Sign out</button></form></div><nav className="mx-auto flex max-w-7xl gap-1 overflow-x-auto px-3 pb-3 md:hidden"><Link href="/client" className="shrink-0 rounded-full bg-background px-4 py-2 text-sm">Today</Link><Link href="/client/workout" className="shrink-0 rounded-full bg-background px-4 py-2 text-sm">Workout</Link><Link href="/client/nutrition" className="shrink-0 rounded-full bg-background px-4 py-2 text-sm">Nutrition</Link><Link href="/client/check-ins" className="shrink-0 rounded-full bg-background px-4 py-2 text-sm">Check-ins</Link></nav></header>{children}</div>; }
