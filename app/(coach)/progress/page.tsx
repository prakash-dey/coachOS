import Link from "next/link"; import { redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server";
export default async function ProgressPage() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/login"); const { data: workspace } = await supabase.from("workspaces").select("id").eq("owner_id", user.id).maybeSingle(); if (!workspace) redirect("/onboarding"); const { data: clients } = await supabase.from("clients").select("id, first_name, last_name, status, check_ins(week_start, weight_kg, energy_score, mood_score)").eq("workspace_id", workspace.id).neq("status", "archived").order("first_name"); return <main className="px-4 py-6 sm:px-6 lg:px-10 lg:py-9"><div className="mx-auto max-w-7xl"><header><p className="text-xs font-bold uppercase tracking-[.2em] text-brand">Progress map</p><h1 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">Every journey, at a glance</h1><p className="mt-2 text-muted">Each path is built from the client’s latest weekly signals.</p></header><div className="mt-8 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{clients?.map((client,index) => { const checks = [...client.check_ins].sort((a,b) => b.week_start.localeCompare(a.week_start)); const latest = checks[0]; const oldest = checks[checks.length-1]; const weightChange = latest?.weight_kg && oldest?.weight_kg ? Number(latest.weight_kg)-Number(oldest.weight_kg) : null; const values = checks.slice(0,8).reverse().map(c => c.energy_score); const points = values.length > 1 ? values.map((v,i) => `${(i/(values.length-1))*100},${42-(v/5)*36}`).join(" ") : "0,24 100,24"; const colors=["bg-[#e7ebff]","bg-[#fff0e7]","bg-[#e4f4de]"]; return <Link key={client.id} href={`/clients/${client.id}/check-ins`} className={`overflow-hidden rounded-[2rem] border border-black/5 ${colors[index%colors.length]} p-6 transition hover:-translate-y-1 hover:shadow-xl`}><div className="flex items-center justify-between"><div><p className="text-xl font-semibold">{client.first_name} {client.last_name}</p><p className="mt-1 text-xs font-bold uppercase tracking-wider text-muted">{checks.length} check-ins</p></div><span className="grid h-11 w-11 place-items-center rounded-full bg-white/70 font-bold">{client.first_name[0]}{client.last_name[0]}</span></div><svg aria-label="Recent energy trend" viewBox="0 0 100 48" preserveAspectRatio="none" className="mt-7 h-24 w-full overflow-visible"><path d="M0 42H100" stroke="rgba(0,0,0,.12)"/><polyline points={points} fill="none" stroke="var(--brand)" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" vectorEffect="non-scaling-stroke"/></svg><div className="mt-4 flex items-end justify-between border-t border-black/10 pt-4"><div><p className="text-xs text-muted">Latest energy</p><p className="text-2xl font-semibold">{latest?.energy_score ?? "—"}<span className="text-sm">/5</span></p></div><div className="text-right"><p className="text-xs text-muted">Weight journey</p><p className="font-semibold">{weightChange === null ? "Not enough data" : `${weightChange > 0 ? "+" : ""}${weightChange.toFixed(1)} kg`}</p></div></div></Link>; })}</div>{!clients?.length && <div className="mt-8 rounded-[2rem] border border-dashed border-brand/30 bg-surface p-10 text-center"><h2 className="text-xl font-semibold">No journeys to map yet</h2><p className="mt-2 text-sm text-muted">Add clients and collect weekly check-ins to reveal progress patterns.</p></div>}</div></main>; }
