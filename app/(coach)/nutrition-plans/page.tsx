import Link from "next/link"; import { redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server";
export default async function NutritionPlansPage() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/login"); const { data: workspace } = await supabase.from("workspaces").select("id, name").eq("owner_id", user.id).maybeSingle(); if (!workspace) redirect("/onboarding"); const { data: plans, error } = await supabase.from("nutrition_plans").select("id, name, description, status, daily_calories, protein_grams, carbs_grams, fat_grams, nutrition_meals(count), nutrition_plan_assignments(count)").eq("workspace_id", workspace.id).order("updated_at", { ascending: false }); if (error) throw new Error("Unable to load nutrition plans."); return <main className="px-4 py-6 sm:px-6 lg:px-10 lg:py-9"><div className="mx-auto max-w-7xl"><header className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-[#b05a28]">Nutrition atelier</p><h1 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">Nutrition plans</h1><p className="mt-2 text-muted">Flexible food frameworks for {workspace.name}.</p></div><Link href="/nutrition-plans/new" className="inline-flex min-h-11 items-center justify-center rounded-full bg-brand px-5 text-sm font-semibold text-white">+ Create plan</Link></header>{plans?.length ? <div className="mt-8 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{plans.map((plan, index) => { const meals = (plan.nutrition_meals as unknown as { count: number }[])?.[0]?.count ?? 0; const assigned = (plan.nutrition_plan_assignments as unknown as { count: number }[])?.[0]?.count ?? 0; const palette = ["bg-[#fff0e7]", "bg-[#f8e8ed]", "bg-[#e4f4de]"]; return <Link href={`/nutrition-plans/${plan.id}`} key={plan.id} className={`group rounded-[2rem] border border-black/5 p-6 ${palette[index % palette.length]} transition hover:-translate-y-1 hover:shadow-xl`}><div className="flex justify-between"><span className="rounded-full bg-white/70 px-3 py-1 text-xs font-bold capitalize">{plan.status}</span><span className="grid h-10 w-10 place-items-center rounded-full bg-white/70">→</span></div><h2 className="mt-7 text-2xl font-semibold">{plan.name}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-muted">{plan.description || "A flexible daily food framework."}</p><div className="mt-6 grid grid-cols-4 gap-2 text-center"><div><b>{plan.daily_calories ?? "—"}</b><small className="block text-[9px] uppercase text-muted">kcal</small></div><div><b>{plan.protein_grams ?? "—"}</b><small className="block text-[9px] uppercase text-muted">protein</small></div><div><b>{meals}</b><small className="block text-[9px] uppercase text-muted">meals</small></div><div><b>{assigned}</b><small className="block text-[9px] uppercase text-muted">assigned</small></div></div></Link>; })}</div> : <div className="mt-8 grid min-h-80 place-items-center rounded-[2rem] border border-dashed border-brand/30 bg-surface p-8 text-center"><div><span className="mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-[#fff0e7] text-2xl">◌</span><h2 className="mt-5 text-xl font-semibold">Create your first food framework</h2><p className="mt-2 text-sm text-muted">Build meals, portions, macros, and flexible alternatives.</p></div></div>}</div></main>; }
