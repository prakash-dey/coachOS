"use client";
import { Button } from "@/app/components/ui/Button";
export default function CoachError({ reset }: { error: Error & { digest?: string }; reset: () => void }) { return <main className="grid min-h-[70vh] place-items-center p-6"><div className="max-w-md rounded-[2rem] border border-border bg-surface p-8 text-center"><span className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-[#fff0e7] text-xl">!</span><h1 className="mt-5 text-2xl font-semibold">That page lost its rhythm</h1><p className="mt-2 text-sm leading-6 text-muted">Your data is safe. Try loading this workspace view again.</p><Button onClick={reset} className="mt-6">Try again</Button></div></main>; }
