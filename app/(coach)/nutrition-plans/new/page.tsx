import Link from "next/link"; import { createNutritionPlan } from "../actions";
export default async function NewNutritionPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const query = await searchParams; return <main className="px-4 py-6 sm:px-6 lg:px-10 lg:py-9"><div className="mx-auto max-w-3xl"><Link href="/nutrition-plans" className="text-sm font-medium text-brand">← Nutrition library</Link><div className="mt-6 overflow-hidden rounded-[2rem] border border-border bg-surface"><div className="bg-[#9a4a21] p-7 text-white sm:p-10"><p className="text-xs font-bold uppercase tracking-[.2em] text-[#ffd8bd]">New food framework</p><h1 className="mt-3 text-3xl font-semibold">Nutrition without the spreadsheet.</h1><p className="mt-2 text-sm text-white/70">Set the daily direction now, then shape meals and flexible food choices.</p></div><form action={createNutritionPlan} className="space-y-6 p-7 sm:p-10">{query.error && <p className="rounded-xl bg-red-50 p-4 text-sm text-red-700">Check the values and try again.</p>}<div><label className="text-sm font-semibold" htmlFor="name">Plan name</label><input id="name" name="name" required maxLength={120} placeholder="Balanced performance plan" className="mt-2 min-h-12 w-full rounded-xl border border-border bg-background px-4"/></div><div><label className="text-sm font-semibold" htmlFor="description">Strategy</label><textarea id="description" name="description" rows={4} className="mt-2 w-full rounded-xl border border-border bg-background p-4" placeholder="How should this plan fit the client’s life?"/></div><div className="grid grid-cols-2 gap-3 sm:grid-cols-4">{[["calories","Calories"],["protein","Protein (g)"],["carbs","Carbs (g)"],["fat","Fat (g)"]].map(([name,label]) => <div key={name}><label className="text-xs font-semibold" htmlFor={name}>{label}</label><input id={name} name={name} type="number" min="0" className="mt-2 min-h-11 w-full rounded-xl border border-border bg-background px-3"/></div>)}</div><button className="min-h-11 rounded-full bg-brand px-6 text-sm font-semibold text-white">Create and add meals</button></form></div></div></main>; }
